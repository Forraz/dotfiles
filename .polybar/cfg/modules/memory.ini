[module/memory]
type = internal/memory
interval = 1

format = <label>
format-foreground = ${colors.fg-alt}
format-prefix = %{T2}RAM %{T-}
format-padding = 2
label-foreground = ${colors.fg}
label = %{T2}%used% / %total%%{T-}

