[module/memory]
type = internal/memory
interval = 1

format = <label>
format-foreground = ${colors.fg-alt}
format-prefix = %{T0}RAM %{T-}
format-padding = 2
label-foreground = ${colors.fg}
label = %{T0}%used% / %total%%{T-}

